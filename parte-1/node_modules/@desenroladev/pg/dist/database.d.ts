import { Connection } from './connection';
export declare class Database {
    private pool;
    constructor();
    getConnection(): Promise<Connection>;
    transaction(): Promise<Connection>;
    query<T>(sql: string, binds?: any[], connection?: Connection): Promise<T[]>;
    find<T>(sql: string, binds?: any[], connection?: Connection): Promise<T>;
    execute<T>(sql: string, binds?: any[], connection?: Connection): Promise<T>;
}
export default Database;
